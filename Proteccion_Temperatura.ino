void Temp() {
/*
  if (digitalRead(Cale) == 1) {
    sensors.requestTemperatures();
    int temperatureC = sensors.getTempCByIndex(0);  // temperatura en C que se mide a traves del sensor
    Serial.print(temperatureC);
    Serial.println("ºC");
    if (temperatureC >= temp_max) {
      Serial.println("TEMPERATURA PELIGROSA");
      Serial.println("APAGO PELTIER");
      digitalWrite(Cale, LOW);
      digitalWrite(LEDFAULT, HIGH);
      interruptCounter = 3;
      Serial.println("Pase por Temperatura");

    }

  }*/
} 
